# Backend API base URL
# Used by the API client (src/lib/api.ts) to prefix all requests.
# In development, leave this empty — Vite's dev proxy handles routing to localhost:3000.
# In production, set this to the full backend URL, e.g. https://api.example.com
VITE_API_BASE=
